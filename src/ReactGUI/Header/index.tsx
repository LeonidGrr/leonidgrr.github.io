import React, { FC } from 'react';
import './index.scss';

const Header: FC = () => {
    return (
        <header>
            <h1>my dev lair</h1>
        </header>
    );
};

export default Header;